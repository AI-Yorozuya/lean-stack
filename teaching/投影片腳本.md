# 認識積木 — 投影片腳本（兩頁學會前後端）

> **對齊課程正本**：ai-yorozuya `docs/5-課程/素材/認識積木_v0.md`（課程第 3 步、**付費起點**）。
> **詞彙一律取自** `docs/1-資產/積木詞彙對照_v0.md`——**別自己造詞**（元件保留英文 table/input/select… ＝「指名」的價值）。
> **教材 sandbox ＝ lean-stack**：上半打開列表頁、下半打開訂單詳細頁 `/orders/:id`。
> 每頁五欄：**目標 / 畫面 / 標註 / 講稿重點 / 對應**。`screens/sNN-*.png` 在 live admin（`:5174`）截。
>
> ⚠️ **sandbox 對應待確認**：`認識積木_v0` 的上半原本指「訂單管理·無狀態」（table＋pagination＋input＋select＋button＋dialog 建立＋dialog 刪除）。本 session 已把 lean-stack 的無狀態訂單頁收掉、新增/編輯改換頁。所以上半的「dialog 新增＋dialog 刪除」現以 **會員/商品列表** 最貼（它們仍走 dialog＋form）；詳細頁判斷用 **訂單詳細頁**。待你定調（見文末）。

---

## S00 · 封面：認識積木 ＝ 兩個頁面學會前後端
- **目標**：一句話定位——**列表頁教「指名」、詳細頁教「判斷」**。這是付費起點。
- **畫面**：標題頁。
- **標註**：—
- **講稿重點**：不教語法/框架（那是 AI 的事）；教你把需求講到 AI 一次做對、又看得出它漏了什麼。
- **對應**：`認識積木_v0`「這一章你會得到」

## S01 · 這一章你會得到
- **目標**：先講清楚產出。
- **畫面**：三條列。
- **標註**：—
- **講稿重點**：① **指名**：需求拆成「頁面＋元件＋API」，一次講對。② **判斷**：看 AI 規劃，抓得出漏掉的關聯/狀態/規則。③ **不用學**：語法、框架、API 風格。
- **對應**：`認識積木_v0`

## S02 · 先認四個詞：API 與鐵則
- **目標**：進列表頁前，先給兩個詞。
- **畫面**：兩張小卡（API / 鐵則）。
- **標註**：—
- **講稿重點**：**API** ＝前後端要資料/送資料的窗口，只有四動作 **讀·建·改·刪**（code 寫 GET/POST/PATCH/DELETE，認得就好）。**鐵則** ＝不管怎麼操作都必須成立的規則（總額=明細加總、退款≤已付、庫存不能負）。
- **對應**：`認識積木_v0`「先認四個詞」、`積木詞彙對照` §一・§二

---

## 上半 · 列表頁（學會指名）

### S03 · 一頁 ＝ 完整增刪查改（七元件四 API）★核心
- **目標**：看穿「列表頁 ＝ 幾個元件 × 四個 API」，每個元件都因一個動作而存在。
- **畫面**：截列表頁（`screens/s03-list.png`），把元件框起來標號（就是「把 input/select/button/table/pagination 都框起來」）。
- **標註**：① table＋pagination ② input＋select＋button ③ 新增 button→dialog＋form ④ 每列 編輯/刪除。
- **動作 → 元件 → API**（正本表，逐字取自 `認識積木_v0`）：

  | 你做的事 | 用到的元件 | 背後的 API |
  |---|---|---|
  | 看全部訂單 | table + pagination | 讀清單 |
  | 篩選 | input + select + button | 讀清單（帶條件） |
  | 新增 | button → dialog + form | 建立 |
  | 編輯 | button → dialog + form | 修改 |
  | 刪除 | button → dialog 確認 | 刪除 |

  → **七個元件、四個 API，全在一頁**。管客戶/商品/員工——同一套，換個名詞。
- **講稿重點**：元件**保留英文**（table/input/select…）＝指名的價值，講 AI 認得的詞。
- **對應**：lean-stack 列表頁（會員/商品/訂單）、`intents/訂單管理.md`、`積木詞彙對照` §三

### S04 · 指名練習：把整句講給 AI
- **目標**：示範一次精準下單（worked example）。
- **畫面**：一句 prompt ＋ AI 一次做對的結果。
- **標註**：—
- **講稿重點**：照唸——「做一個商品列表頁：table 列出、pagination 分頁，上面用 input 和 select 篩選；每列能編輯和刪除，新增用 dialog 表單。」它一次給對，因為每個字它都認得。
- **對應**：`認識積木_v0`「練習：指名下單」

### S05 · 你試：會員管理
- **目標**：把能力交回學員。
- **畫面**：空白的「動作→元件→API」表讓學員填 ＋ 參考答案（翻頁）。
- **標註**：—
- **講稿重點**：同一張表自己填，對參考答案。參考答案教判斷——**「好解不是唯一解」**。
- **對應**：lean-stack `/members`、`認識積木_v0`「你試」

---

## 下半 · 詳細頁（學會判斷）

### S06 · 三問：看一張訂單詳細頁 ★核心
- **目標**：拿到判斷 AI 規劃的 checklist。
- **畫面**：截訂單詳細頁 `/orders/:id`（`screens/s06-detail.png`），三問各指一塊。
- **標註**：框「會員/明細/金額」「狀態列」「總計」三處。
- **三問**（正本表）：

  | 問一句 | 看頁面哪裡 | 這叫什麼 |
  |---|---|---|
  | 有什麼？跟誰有關？ | 會員、明細、金額 | 資料模型 |
  | 能發生什麼？什麼順序？ | 待付款 → 待出貨 → 已出貨 / 已取消 | 狀態與流程 |
  | 什麼必須永遠成立？ | 總額=明細加總、已出貨不能改 | 鐵則 |

- **講稿重點**：這三句就是判斷 checklist。三問 ＝ **承重牆的大眾裁剪**（資料 × 狀態 × 不變量）。
- **對應**：lean-stack `/orders/:id`、`intents/訂單管理.md`、`積木詞彙對照` §四

### S07 · 找碴練習：AI 規劃埋了四個洞
- **目標**：用三問抓漏（**整套教學的關鍵驗證點**）。
- **畫面**：一份 AI 規劃（紙上文件，不在 sandbox）＋ 四個洞。
- **標註**：—
- **講稿重點**：四個洞——訂單沒接明細（資料模型）、狀態少了取消這條路（流程）、已出貨還能改明細（流程）、總額能手填沒鎖加總（鐵則）。抓到幾個 ＝ 你現在的判斷力。
- **對應**：`認識積木_v0`「練習：找碴」

### S08 · 你試：商品詳細頁
- **目標**：換一份、埋不同洞，自己抓。
- **畫面**：另一份規劃 ＋ 參考答案。
- **標註**：—
- **講稿重點**：自己抓，對參考答案。
- **對應**：`認識積木_v0`「你試」

---

## S09 · 收尾 → 打造城堡
- **目標**：收兩個能力、接下一步。
- **畫面**：指名 / 判斷 兩句 ＋ 箭頭 → 打造城堡。
- **標註**：—
- **講稿重點**：**指名** ＝ 讓 AI 一次做對；**判斷** ＝ AI 規劃你看得出哪裡少。下一步用這兩個能力真的蓋東西。
- **對應**：`打造城堡_v0.md`

---

> **教法通則**：worked example →「你試 ＋ 參考答案」；兩個記憶點——第一次精準下單一次做對、第一次抓到 AI 沒說的坑。
> **未出場元件**：tab（詳細頁欄位多時按需教）、分步表單（按需）。
